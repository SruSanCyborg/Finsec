packages:
  - packages/*
allowBuilds:
  # Analytics package pulled in transitively. A security tool has no business
  # phoning home, and nothing here needs its postinstall.
  '@scarf/scarf': false
  # Needs its postinstall to link the platform binary; vitest depends on it.
  esbuild: true
