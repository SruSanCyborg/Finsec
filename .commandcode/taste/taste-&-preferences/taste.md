# Taste & preferences
- Prefers premium, cinematic, interactive product-experience landing pages (Apple Vision Pro / Linear / Vercel / Raycast quality) over conventional SaaS layouts; explicitly rejects the generic hero pattern of headline + subtitle + two buttons + background particles + feature cards. Confidence: 0.95
- Wants the product demonstrated rather than described — the page should let visitors *experience* the product's core loop through scroll-driven storytelling, not merely tell them what it does. Confidence: 0.9
- Treats reference sites (SharpLink, Oxigen, Van Lent, Linear, Vercel, Apple) as sources of interaction quality, pacing, and motion only — wants an original identity, never copied design, assets, or branding. Confidence: 0.9
- Cares about a high differentiation/wow quality bar — the result should feel like an interactive product launch, not "a nice SaaS website." Confidence: 0.9
- Assigns clear animation-library responsibilities: GSAP (+ ScrollTrigger) for scroll choreography, camera, timelines and counters; React Three Fiber / Three.js for the 3D scene; Framer Motion only for small UI transitions (menus, buttons, dialogs). Avoids using multiple libraries for the same animation. Confidence: 0.9
- Prefers Clerk for authentication (sign-in, sign-up, sessions, organizations, roles, permissions) — explicitly "do not build custom authentication"; secret credentials must stay server-side, the browser only ever sees the publishable key. Confidence: 0.95
- Performance-first for immersive/3D experiences: lazy-load Three.js and heavy assets so they never block first render, use instancing and compressed geometry, cap devicePixelRatio, tier scene complexity by device, and provide a graceful fallback when WebGL is unavailable. Confidence: 0.9
- On mobile/weak devices, build a simplified 3D scene rather than shrinking the desktop one — reduce particles, geometry, post-processing and camera movement while preserving the storytelling. Confidence: 0.9
- Respects `prefers-reduced-motion`: reduce camera movement, disable scroll-scrubbed animations and parallax, cut particle counts, and keep content readable. Confidence: 0.9
- Provides detailed, prescriptive briefs with explicit "do not" lists and a concrete quality bar, and expects the codebase to be inspected before writing code (inspect first, understand the existing architecture, don't rewrite working code unnecessarily). Confidence: 0.9
- Runs the agent in "yolo" mode (auto-approve): wants autonomous execution of commands and changes without per-step confirmation prompts; re-issues `cmd --yolo` to keep the agent going without adding new instructions. Confidence: 0.9
- When something appears broken or behaves unexpectedly, wants a plain-language explanation of what is actually happening and why (the root-cause mechanism), not just a fix — explain in simple, non-jargon terms with concrete next steps. Confidence: 0.75
- Communicates informally and with emotional/urgent tone when blocked or frustrated (typos like "sihn in", "what to doooo??", "at all", "s=is", all-caps "OK DID U CONNECT TO CLI ???", "SO U CONNECTED TO THE CLI IN THE CLI BRANCH AND AUTH FOR CLERK", "still notv working") — expects a direct, plain-language, actionable answer with concrete next steps, not formal or lengthy framing. Confidence: 0.9
- A custom helper script is not an acceptable substitute for the real product tool — when asked whether the CLI is connected, expects the actual `sirius` CLI from its branch/worktree to be built and wired to the backend (doctor + hosted scan verified), not a one-off push script. Confidence: 0.7
- Reports bugs by pasting the raw error/console message — or even a full paste of the rendered page content — with little or no context (e.g., "Failed to load resource: the server responded with a status of 404"; pasted an entire landing-page text dump; pasted the login page's full rendered fallback text to report that Clerk sign-in wasn't showing) and expects the agent to investigate and diagnose the root cause itself rather than asking for details. Confidence: 0.75
- Wants the dev server started with `npm run dev` (npm commands), even though the repo is set up for pnpm — use npm when invoking run/dev scripts on the user's behalf. Confidence: 0.6
- When asking how to run something, wants the exact, copy-paste-ready command verbatim in a code block — not a paraphrased explanation of what to type. Confidence: 0.6
- Extends the "unlike anything seen before" quality bar beyond landing pages to feature implementations (e.g., the PDF report generator) — core features should also feel exceptional, not just the marketing surface. Confidence: 0.6
- Expects previously shared configuration (e.g., a Neon connection string given "earlier") to be remembered or found in the repo rather than re-asked for; wants the agent to continue from prior context instead of starting over. Confidence: 0.7
- Wants the whole stack wired end-to-end — demo auth account, backend, and database all configured and connected ("all must be configured"), not left as placeholders or mock-only stubs; every backend surface should be connected to the frontend ("connect all other features of backend to frontend every single thing u can think of"). Confidence: 0.8
- When sign-in/sign-up is broken, wants it made to work immediately with whatever is available (backend demo auth fallback) rather than left broken pending missing credentials; real auth (Clerk) stays as the primary path once keys exist, with the demo clearly labeled as secondary. Confidence: 0.75
- Wants the CLI/local tool connected to the web app and database as one pipeline ("CLI properly connected to the web") — local scans push into the hosted store (Neon) via an API ingest endpoint so the web console shows the same data, analytics, and reports as local runs. Confidence: 0.8
- Prefers live real-time updates (WebSockets/SSE) between backend and web — a global live-event stream and per-scan streams so the dashboard refreshes when a CLI scan lands, instead of manual polling or reload. Confidence: 0.75
- Wants a clean authentication/connection foundation documented and tested rather than half-built: public vs protected routes with proper 401s, a shared auth middleware (no duplicated per-route auth logic), a `/health` liveness endpoint with a small frontend connection indicator, standardized API error shapes, env vars documented in `.env.example` (placeholders only, secrets never committed or exposed), and a documented CLI auth foundation (device-flow, no Clerk secret in the CLI). Confidence: 0.8
- Cares that surface behavior matches across tools (CLI, web, backend): the hosted engine, rule catalogue, money model and PDF writer should be ports of the CLI's local engine so every surface produces the same numbers. Confidence: 0.7
- Values drop-in API compatibility for official clients over forcing client changes: when wiring the real CLI to the backend, adds missing root-level `/healthz` (and `/readyz`) aliases, accepts both `Authorization: Bearer` header and `?token=` query on WebSocket upgrades, and closes the scan stream after the `scan.completed` frame so the CLI can exit cleanly. Confidence: 0.8
- Documents exact working invocation commands for tool integration (REST base must include `/api/v1`, WS origin must not — the CLI appends it) in a dedicated `scripts/CLI_CONNECT.md`, including Windows cmd env-var caveats, so the connection is repeatable rather than tribal knowledge. Confidence: 0.75
- Tolerates pragmatic environment-specific workarounds when a clean fix is unavailable (e.g., running the app in production mode or pre-compiling Tailwind because Next dev + pnpm is broken on this machine), as long as the app actually works and the caveat is stated plainly. Confidence: 0.7
- Writes acceptance/verification test scripts (e.g., `scripts/test_auth.py`) that assert each acceptance criterion (health, 401s, /me, team, CORS allow/reject, no duplicate users) and expects all checks to pass before reporting done. Confidence: 0.75
- Wants the product's identity/name applied consistently everywhere — when corrected on the app name, expects the rename propagated through package metadata, UI text, landing copy, seed data, env examples, docs, and demo credentials, not just one spot. Confidence: 0.75
- Pastes live/production credentials directly into chat (e.g., Clerk `pk_live_`/`sk_live_` keys "for dev mode") and expects them wired into the correct env files immediately — publishable key in the frontend env, secret key in the backend env — rather than test keys, placeholders, or being asked to configure them himself. Confidence: 0.6
- Wants the agent's own session/tool data (e.g., `.commandcode`) added to `.gitignore` so the repo stays clean of agent artifacts — explicitly requests this repo-hygiene step rather than leaving it uncommitted-tracked. Confidence: 0.55
0.6
